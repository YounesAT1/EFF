/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "images.kiwi.com",
      "img.mobiscroll.com",
      "localhost",
    ],
  },
};

module.exports = nextConfig;
